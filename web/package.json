{
  "name": "hydro-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview"
  },
  "comment": "Dependencies and full toolchain (React, Tailwind, Recharts, Vite, Biome) are added in Stage 5."
}
